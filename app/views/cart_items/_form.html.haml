= simple_form_for @cart_item, url: cart_item_path, html: { class: 'form-horizontal' } do |f|
  - if notification = f.error_notification
    = notification

  = f.input :quantity

  .form-actions
    = f.button :submit, class: 'btn-primary'
