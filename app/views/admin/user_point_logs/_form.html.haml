= simple_form_for [:admin, @user, @user_point_log], html: { class: 'form-horizontal' } do |f|
  - if notification = f.error_notification
    = f.error_notification

  = f.input :change_point
  = f.input :description

  .form-actions
    = f.button :submit, class: 'btn-primary'
